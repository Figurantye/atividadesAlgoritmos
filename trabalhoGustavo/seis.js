function main(){
    console.log(displayAlertMessage("Figurante", "Windows"));
}

function displayAlertMessage(usuario, so){
return `There's a new sign-in request on ${usuario} for your Google Account ${so}.`
}

main()